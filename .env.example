# =========================
# jework 环境变量示例
# =========================

# 服务端口（默认 9508）
PORT=9508

# 统一数据目录（会自动创建）
# 目录结构：
# - ${DATA_DIR}/workspaces
# - ${DATA_DIR}/db/app.db
DATA_DIR=./data

# 前端静态资源目录（Flutter Web 构建产物）
FRONTEND_STATIC_DIR=./app/static

# Claude Agent 行为配置
# 每次请求的最大循环轮次（默认 20）
CLAUDE_AGENT_MAX_TURNS=20

# 允许的工具列表（逗号分隔），留空走后端默认值
# 例如：Read,Glob,Grep,WebSearch,WebFetch,AskUserQuestion
CLAUDE_AGENT_ALLOWED_TOOLS=

# =========================
# 可选：Claude/Anthropic 运行时配置
# 说明：通常建议在页面「模型配置」中维护，这里仅做兜底
# =========================
ANTHROPIC_BASE_URL=
ANTHROPIC_AUTH_TOKEN=
ANTHROPIC_MODEL=
ANTHROPIC_DEFAULT_SONNET_MODEL=
ANTHROPIC_DEFAULT_HAIKU_MODEL=
ANTHROPIC_DEFAULT_OPUS_MODEL=
